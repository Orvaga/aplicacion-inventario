# Backend

Carpeta para lógica del servidor y scripts relacionados con la base de datos.

- `database_schema.sql` — DDL para crear `database.db`.
- `create_database.py` — Script para crear/actualizar `database.db` desde el SQL.

Coloca aquí el código del backend (APIs, servicios, modelos, migraciones, etc.).
